// function tenTimes(number){
//     const result = number * 10;
//     return result;
// }

// tenTimes(5)

// Ans: emty কিছুই আসবে না। কারণ return ওলা কোনো মানকে দেখতে হলে ওই function কে একটা ভেরিয়েবল এর মধ্যে রাখতে হবে। 
// তারপর ওই ভেরিয়েবলের নাম ধরে console.log করে দেখতে হবে। চলো এটা দেখা যাক।


// const output = tenTimes(5);
// console.log('Output:',output)

// Ans:         Output: 50


//  so we can see more example look like this type. we can do anythink(+,-,*,/)


function add(num1, num2){
    const additional = num1 + num2;
    return additional;
}

const total = add(30,35);
console.log('Tatal:', total)

// Ans:     Tatal: 65





function div(number){
    const divide = number / 2;
    return divide;
}

const dividation = div(70);
console.log ('Answer:', dividation)


// Ans:     Answer: 35